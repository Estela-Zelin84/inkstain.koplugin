-- 墨痕壁纸 OTA 配置
return {
    repo = "Estela-Zelin84/inkstain.koplugin",
    plugin_dir = "inkstain.koplugin",
    plugin_id = "inkstain",
    channel = "stable",
    channel_tag = "stable-channel",

    -- 可选 GitHub 下载镜像，默认关闭
    github_mirrors = {},
}
